engine-strict=true

# Override the machine-wide corporate registry (a JFrog proxy) for this project specifically.
# This is a personal open-source project, not work code, and the proxy has been unreliably slow.
registry=https://registry.npmjs.org/
